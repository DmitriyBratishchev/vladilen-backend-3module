<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <title>
    <%= title %>
  </title>
</head>

<body>
  <div class="container">
    <h1>Мои заметки. <%= title %>
    </h1>
    <form action="/" method="post">
      <div class="form-group mb-2">
        <label for="title">Название заметки</label>
        <input type="text" class="form-control" name="title" required>
      </div>
      <button class="btn btn-primary mb-2">Создать!</button>
    </form>

    <% if(notes.length> 0) {%>
      <div class="card">
        <ul class="list-group">
          <% for (let i=0; i < notes.length; i++) { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <%= notes[i].title %>
                <button class="btn btn-primary ms-auto" data-type="edit" data-id="<%= notes[i].id%>"
                  data-title="<%= notes[i].title %>">Редактировать</button>
                <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id%>">&times;</button>
            </li>
            <% } %>
        </ul>
      </div>
      <%} else {%>
        <p>Список пуст! Создайте свою первую заметку.</p>
        <%}%>

  </div>
  <script src="/app.js"></script>
</body>

</html>